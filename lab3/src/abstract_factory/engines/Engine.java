package abstract_factory.engines;

/**
 * Interface Engine
 * 
 * Define o método start que será implementado pelas classes que a implementam,
 * para criar um motor de carro.
 */
public interface Engine {
    void start();
}
